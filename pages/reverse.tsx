// Reverse of speech-to-text.tsx: xi38 text -> speech.
// This is exactly what the home page (pages/index.tsx) already does, so
// it's re-exported here as its own dedicated route rather than duplicated.
export { default } from "./index";
